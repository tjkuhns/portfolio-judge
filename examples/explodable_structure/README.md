# Structural scan — Explodable

```bash
portfolio-judge structure /home/thoma/explodable
```

Deterministic — no LLM calls, no API key needed, $0 cost. Ran in <1 second. Output in [`scan.md`](scan.md).

**Date:** 2026-04-19

**Headline:** 10/12 checks passed. Moderate failures: missing LICENSE at root, missing "usage" section keyword in README. Everything else green.

This output is the fastest way to understand what `portfolio-judge` can tell you about a repo before spending any money on LLM calls.
