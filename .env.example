# At least one of these is required — whichever provider you pass via --model
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Optional: raises GitHub API rate limit from 60 req/hr to 5000 req/hr.
# Required for reviewing private repos (not supported yet).
GITHUB_TOKEN=
